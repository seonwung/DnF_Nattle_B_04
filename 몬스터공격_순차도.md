```mermaid
sequenceDiagram
    actor Player as 플레이어
    participant UI as Attack_Monster_UI.jsp
    participant Session as 세션
    participant Battle as 전투
    participant PlayerClass as 플레이어
    participant Char as 캐릭터

    Player->>UI: 던전 입장 및 몬스터 공격 요청

    UI->>Session: getAttribute("myDnFCharacter")
    Session-->>UI: return character

    UI->>Battle: new 전투()
    UI->>Battle: 몬스터공격(플레이어id, character)

    Battle->>PlayerClass: 플레이어체크(플레이어id)

    alt 플레이어id == "hero"
        PlayerClass-->>Battle: return true

        alt character == null
            Battle-->>UI: return "생성된 캐릭터가 없습니다."
            UI-->>Player: 캐릭터 없음 메시지 출력

        else character != null
            Battle->>Char: 스킬발동()
            Char-->>Battle: return 데미지

            Battle->>Char: get캐릭터명()
            Char-->>Battle: return 캐릭터명

            Battle->>Char: get스킬명()
            Char-->>Battle: return 스킬명

            alt 데미지 >= 200
                Battle->>Battle: 등급 = "S급 공격"
            else 데미지 >= 100
                Battle->>Battle: 등급 = "A급 공격"
            else 데미지 < 100
                Battle->>Battle: 등급 = "B급 공격"
            end

            Battle-->>UI: return 전투결과
            UI-->>Player: 전투결과 화면 출력
        end

    else 플레이어id != "hero"
        PlayerClass-->>Battle: return false
        Battle-->>UI: return "플레이어 권한이 없습니다."
        UI-->>Player: 권한 없음 메시지 출력
    end
````